# PUBLIC env for the frontend
# IMPORTANT: Ask the user to provide this value for deployment/environments.
# This value should point to the accounting_backend base URL (e.g., http://localhost:4000 or https://api.example.com)
NEXT_PUBLIC_API_BASE_URL=http://localhost:4000
