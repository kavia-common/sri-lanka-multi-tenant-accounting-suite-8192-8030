# PUBLIC INTERFACE - Required environment variables for the frontend
# The base URL of the backend API (e.g., http://localhost:3001)
NEXT_PUBLIC_API_BASE_URL=http://localhost:3001

# Site URL for auth redirects (used in email flows if implemented later)
NEXT_PUBLIC_SITE_URL=http://localhost:3000
